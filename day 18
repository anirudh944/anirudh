<div id="a"></div>
    <script type="text/babel">
        function getGreeting(name){
            return `Hello, ${name}! welcome to React`;
        }
        function Greeting(){
            const name="anirudh"
            return <h1>{getGreeting(name)}</h1>

        }
        const root =ReactDOM.createRoot(document.getElementById('a'))
        root.render(<Greeting/>)
    </script>







    <!-- <div id="root"></div>
    <script type="text/babel">
       const App=()=>{
        const isloggedIn=true;
        return(
            <div>
               { isloggedIn?(<h1>welcome back,user </h1>):(<h1> please log in</h1>)}

               { isloggedIn&&<h1>you have a new notification </h1>}
            </div>

        )
        
       }
       const root =ReactDom.createRoot(document.getElementById('root'))
       root.render(<App/>)
       
      </script> -->
    <!-- <script type="text/babel"> 
        const element=<h1 className="aa">welcome to chiru solutions </h1>
        const e=document.getElementById('a')
        const root =ReactDOM.createRoot(e)
        root.render(element) -->
    <!-- </script> -->
    <!-- <script>
        const b=React.createElement('h1',{id:'c'},"Iam Venky")
        const e=document.getElementById('a')
        const root=ReactDOM.createRoot(e)
        root.render(b)
    </script> -->
